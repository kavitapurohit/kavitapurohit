/*var server = require('./lib/server');
var workers = require('./lib/workers');
var cli = require ('./lib/cli');
var cluster = require('cluster');
var os = require('os');



var app = {};
app.init = funcation(callback) {

    if(cluster.ismaster){

workers.init();
setTimeout(function ()) {
    cli.init();
    callback();
},50);
    
})*/
    
